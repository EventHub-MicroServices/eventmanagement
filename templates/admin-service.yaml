{{- if .Values.admin.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace }}
{{- end }}
---
{{- if .Values.admin.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-config
  namespace: {{ .Values.global.namespace }}
data:
  NODE_ENV: {{ .Values.admin.config.NODE_ENV | quote }}
  PORT: {{ .Values.admin.config.PORT | quote }}
  DB_HOST: {{ .Values.admin.config.DB_HOST | quote }}
  DB_PORT: {{ .Values.admin.config.DB_PORT | quote }}
  DB_NAME: {{ .Values.admin.config.DB_NAME | quote }}
  JWT_EXPIRES_IN: {{ .Values.admin.config.JWT_EXPIRES_IN | quote }}
{{- end }}
---
{{- if .Values.admin.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: admin-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  DB_USER: {{ .Values.admin.secrets.DB_USER | b64enc }}
  DB_PASSWORD: {{ .Values.admin.secrets.DB_PASSWORD | b64enc }}
  JWT_SECRET: {{ .Values.admin.secrets.JWT_SECRET | b64enc }}
{{- end }}
---
{{- if .Values.admin.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - port: {{ .Values.admin.service.port }}
      targetPort: {{ .Values.admin.service.targetPort }}
      protocol: TCP
  type: {{ .Values.admin.service.type }}
{{- end }}
---
{{- if .Values.admin.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-deployment
  namespace: {{ .Values.global.namespace }}
  labels:
    app: admin-service
spec:
  replicas: {{ .Values.admin.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: "{{ .Values.admin.image.repository }}:{{ .Values.admin.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.admin.service.targetPort }}
          envFrom:
            - configMapRef:
                name: admin-config
            - secretRef:
                name: admin-secret
          resources:
            requests:
              cpu: {{ .Values.admin.resources.requests.cpu | quote }}
              memory: {{ .Values.admin.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.admin.resources.limits.cpu | quote }}
              memory: {{ .Values.admin.resources.limits.memory | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.admin.service.targetPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.admin.service.targetPort }}
            initialDelaySeconds: 30
            periodSeconds: 10
{{- end }}
